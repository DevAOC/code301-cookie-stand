export default function Header() {
  return (
    <header
      style={{
        height: '100px',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: 'plum',
        marginBottom: '15px',
      }}
    >
      <h1>Cookie Stand Admin</h1>
    </header>
  );
}
